# Server configuration
PORT=5000

# Database configuration (PostgreSQL)
DB_HOST=localhost
DB_PORT=5432
DB_NAME=jarvis_ticketing
DB_USER=postgres
DB_PASSWORD=your_secure_password

# Authentication (JWT secret key)
JWT_SECRET=your_jwt_secret_key_here
